; Hardware specifications on which we are running, required for proper scaling
[hardware:screen]
    width = 1024
    height = 600

; GUI settings
[application:gui]
    background = #295864
    fontcolor = #fdf6e3
    xpos = 0
    ypos = 0

; use double percent signs to escape interpolation!
[application:core]
    timeformat = %%H:%%M:%%S
    dateformat = %%d.%%m.%%Y
    ;loggingformat = %%(asctime)s - %%(threadName)s - %%(levelname)s - %%(message)s
    loggingformat = %%(asctime)s - %%(threadName)s - %%(name)s - %%(levelname)s - %%(message)s

    ; possible values: celsius, fahrenheit
    temperatureunit = celsius

    ; the amount of decimal points for temperature, pressure
    valueaccuracy = 2